# tcpcount
Utility to list active TCP port connections with per-IP counts, using caching for faster rendering. Works on Linux and FreeBSD, and requires whois to be installed.

usage ./tcpcount.sh [port]
